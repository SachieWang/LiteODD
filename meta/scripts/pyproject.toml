[project]
name = "methodology-checker"
version = "1.0.0"
description = "Methodology Layer 0 compliance checker (three invariants)"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0",
  "jsonschema>=4.0",
]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
